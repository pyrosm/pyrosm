"""Vendored protobuf message modules generated from ``proto/*.proto``.

The ``*_pb2.py`` modules here are produced by ``protoc`` and committed so that
building pyrosm needs no protobuf compiler. Regenerate with
``scripts/regenerate_proto.py`` after editing the ``.proto`` sources.
"""
