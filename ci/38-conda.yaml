name: test

channels:
  - conda-forge

dependencies:
  - python=3.8
  # required
  - geopandas
  - pygeos
  # testing
  - pytest
  - pytest-cov
  - codecov
  # pyrobuf needs to be built temporarily from PyPi, as latest version is not pushed to conda-forge yet.
  - pip
  - pip:
      - pyrobuf

