name: test

channels:
  - conda-forge

dependencies:
  - python=3.7
  # required
  - geopandas
  - pygeos
  - cython
  - pyrobuf
  # testing
  - pytest
  - pytest-cov
  - codecov
  - pip
  - pip:
      - git+https://github.com/realead/cykhash.git

