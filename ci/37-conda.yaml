name: test

channels:
  - conda-forge

dependencies:
  - python=3.7
  # required
  - geopandas
  - pygeos
  - cython
  # testing
  - pytest
  - pytest-cov
  - codecov
  # pyrobuf needs to be built temporarily from PyPi, as latest version is not pushed to conda-forge yet.
  - pip
  - pip:
      - pyrobuf
      - git+https://github.com/realead/cykhash.git

